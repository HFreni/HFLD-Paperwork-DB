<%= form_with(model: spotlight, local: true) do |form| %>
  <% if spotlight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spotlight.errors.count, "error") %> prohibited this spotlight from being saved:</h2>

      <ul>
      <% spotlight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :spotlight_name %>
    <%= form.text_field :spotlight_name %>
  </div>

  <div class="field">
    <%= form.label :spotlight_op_fk %>
    <%= form.number_field :spotlight_op_fk %>
  </div>

  <div class="field">
    <%= form.label :spotlight_model_fk %>
    <%= form.number_field :spotlight_model_fk %>
  </div>

  <div class="field">
    <%= form.label :spotlight_notes %>
    <%= form.text_area :spotlight_notes %>
  </div>

  <div class="field">
    <%= form.label :spotlight_position_fk %>
    <%= form.number_field :spotlight_position_fk %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
