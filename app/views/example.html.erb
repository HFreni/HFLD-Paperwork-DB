<section class="section">
  <div class="container">
    <h1 class="title">New Color</h1>
    <%= form_with scope: :color, url: colors_path, local: true do |form| %>

      <div class="field">
        <%= form.label :gel_num, :class => "label" %>
        <div class="control">
          <%= form.text_field :gel_num, :class =>"input" %>
        </div>
      </div>

      <div class="field">
        <%= form.label :gel_name, :class => "label" %>
        <div class="control">
          <%= form.text_area :gel_name, :class =>"input" %>
        </div>
      </div>

      <div class="field">
        <%= form.label :gel_hex, :class => "label" %>
        <div class="control">
          <%= form.text_area :gel_hex, :class =>"input" %>
        </div>
      </div>

      <div class="field">
        <%= form.label :manufacturer_id, :class => "label" %>
        <div class="control">
          <%= form.text_area :manufacturer_id, :class =>"input" %>
        </div>
      </div>

      <div class="field">
        <%= form.submit :class => "button is-link" %>
      </div>
    <% end %>
  </div>
</section>

<%= render 'form', spotlight: @spotlight %>


  <%= form_with(model: spotlight, local: true) do |form| %>
    <% if spotlight.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(spotlight.errors.count, "error") %> prohibited this spotlight from being saved:</h2>

        <ul>
        <% spotlight.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :spotlight_name, :class => "label"  %>
      <%= form.text_field :spotlight_name, :class =>"input" %>
    </div>

    <div class="field">
      <%= form.label :spotlight_op_fk, :class => "label"  %>
      <%= form.number_field :spotlight_op_fk, :class =>"input" %>
    </div>

    <div class="field">
      <%= form.label :spotlight_model_fk, :class => "label"  %>
      <%= form.number_field :spotlight_model_fk, :class =>"input" %>
    </div>

    <div class="field">
      <%= form.label :spotlight_notes, :class => "label"  %>
      <%= form.text_area :spotlight_notes, :class =>"input" %>
    </div>

    <div class="field">
      <%= form.label :spotlight_position_fk, :class => "label"  %>
      <%= form.text_field :spotlight_position_fk, :class =>"input" %>
    </div>

    <table class="table is-fullwidth">
      <thead>
        <tr>
        </tr>
      </thead>
      <tbody>
        <% form.fields_for :color_frames do |color_frame_form| %>
          <% spotlight.color_frames.each {|color_frame| %>
      <tr>
                <th><%=  color_frame_form.text_field :id, multiple: true, :class => "input" %></th>
        <th><%=  color_frame.color.gel_num  %></th>
        <th style="background-color: <%= color_frame.color.gel_hex %> ;"><%=  color_frame.color.gel_name  %></th>
      </tr>
      <%  } %>
        <% end %>
      </tbody>
    </table>

    <div class="actions">
      <%= form.submit :class => "button is-link" %>
    </div>
  <% end %>



  <%= form_with(model: spotlight, local: true) do |f| %>
    <% if spotlight.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(spotlight.errors.count, "error") %> prohibited this spotlight from being saved:</h2>

        <ul>
        <% spotlight.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :spotlight_name, :class => "label"  %>
      <%= f.text_field :spotlight_name, :class =>"input" %>
    </div>

    <div class="field">
      <%= f.label :spotlight_op_fk, :class => "label"  %>
      <%= f.number_field :spotlight_op_fk, :class =>"input" %>
    </div>

    <div class="field">
      <%= f.label :spotlight_model_fk, :class => "label"  %>
      <%= f.number_field :spotlight_model_fk, :class =>"input" %>
    </div>

    <div class="field">
      <%= f.label :spotlight_notes, :class => "label"  %>
      <%= f.text_area :spotlight_notes, :class =>"input" %>
    </div>

    <div class="field">
      <%= f.label :spotlight_position_fk, :class => "label"  %>
      <%= f.text_field :spotlight_position_fk, :class =>"input" %>
    </div>

    <table class="table is-fullwidth">
      <thead>
        <tr>
        </tr>
      </thead>
      <tbody>
      </tbody>
    </table>

    <div class="actions">
      <%= f.submit :class => "button is-link" %>
    </div>
  <% end %>

  <%= form_with(model: spotlight, local: true) do |f| %>
  <% if spotlight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spotlight.errors.count, "error") %> prohibited this spotlight from being saved:</h2>
      <ul>
        <% spotlight.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :spotlight_name, :class => "label"  %>
    <%= f.text_field :spotlight_name, :class =>"input" %>
  </div>

  <div class="field">
    <%= f.label :spotlight_op_fk, :class => "label"  %>
    <%= f.number_field :spotlight_op_fk, :class =>"input" %>
  </div>

  <div class="field">
    <%= f.label :spotlight_model_fk, :class => "label"  %>
    <%= f.number_field :spotlight_model_fk, :class =>"input" %>
  </div>

  <div class="field">
    <%= f.label :spotlight_notes, :class => "label"  %>
    <%= f.text_area :spotlight_notes, :class =>"input" %>
  </div>

  <div class="field">
    <%= f.label :spotlight_position_fk, :class => "label"  %>
    <%= f.text_field :spotlight_position_fk, :class =>"input" %>
  </div>

  <p class="subtitle"> Color Frames</p>
  <ul>
    <% @color_frames.each do |frame| %>
    
    <% end %>
  </ul>

   <%= link_to_remove_association "Remove book", f %>

  <%= f.submit %>
<% end %>